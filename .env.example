# Local dev: SQLite file on disk, no network needed.
DATABASE_URL=file:local.db

# Production (Vercel): Turso database URL + auth token.
# DATABASE_URL=libsql://<db-name>-<org>.turso.io
# DATABASE_AUTH_TOKEN=<turso-auth-token>
